{
  "name": "libraries_backend",
  "version": "1.0.0",
  "description": "backend server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --3000"
  },
  "author": "Drozd A.A.",
  "license": "MIT",
  "devDependencies": {
    "json-server": "^0.12.1"
  }
}
